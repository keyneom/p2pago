# GitHub Pages demo

Serves at `https://keyneom.github.io/p2pago/` when GitHub Pages is enabled (Settings → Pages → Source: Deploy from branch → Branch: main, folder: /docs).

**Update the bundle after changes:**
```bash
npm run build:docs
```
